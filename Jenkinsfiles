pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                git branch
                git branch -a
                git remote -v
            }
        }
    }
}
